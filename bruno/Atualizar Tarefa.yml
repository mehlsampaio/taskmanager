info:
  name: Atualizar Tarefa
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:8080/tarefas?id=1
  params:
    - name: id
      value: "1"
      type: query
  body:
    type: json
    data: |-
      {
        "nome": "Estudar Spring Boot - Atualizada"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
