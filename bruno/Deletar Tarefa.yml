info:
  name: Deletar Tarefa
  type: http
  seq: 4

http:
  method: DELETE
  url: http://localhost:8080/tarefas?id=2
  params:
    - name: id
      value: "2"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
